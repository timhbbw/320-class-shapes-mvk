package ch.bbw.classes.shapes;

public class Fuu {
    static void main() {
        var edgar = new Edgar();
        System.out.println("health" + edgar.getLife());
        edgar.doAttack(edgar);
    }
}
