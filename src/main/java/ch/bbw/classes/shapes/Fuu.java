package ch.bbw.classes.shapes;

public class Fuu {
    static void main() {
        var edgar = new Brawler(10000);

        var health = edgar.getLife();

        edgar.takeDamage(67);

        edgar.toString();
    }
}
